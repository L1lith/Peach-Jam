{
  "name": "scripts",
  "version": "1.0.0",
  "description": "place to hold random various scripts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "pngjs": "^6.0.0"
  }
}
